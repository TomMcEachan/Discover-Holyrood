name: Test Runner

on: [push]

jobs:
  End-to-End:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Install dependencies
        uses: cypress-io/github-action@v5
        with:
          working-directory: apps/client
          runTests: false
          build: npx cypress info

      - name: Cypress run
        uses: cypress-io/github-action@v5
        with:
          working-directory: apps/client
          build: npx cypress run --headless --browser chrome
          record: true
          parallel: true
